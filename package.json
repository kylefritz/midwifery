{
  "name": "midwifery-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "randomcolor": "^0.5.3",
    "react": "^16.1.0",
    "react-dom": "^16.1.0"
  },
  "devDependencies": {
    "node-sass": "^4.6.1",
    "react-scripts": "5.0.1",
    "sass-loader": "^13.2.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "aws s3 sync build/ s3://midwifery.xxx --acl public-read",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
